name: 🎬 Full Vertical Video (Stocks + Voice) - NO IMAGEMAGICK

on:
  workflow_dispatch:
    inputs:
      topic:
        description: 'Topic for video'
        required: true
        default: '20 local AI agents free 48 hours'
      voice:
        description: 'Voice'
        required: true
        default: 'ru-RU-DmitryNeural'
        type: choice
        options:
          - ru-RU-DmitryNeural
          - ru-RU-SvetlanaNeural
          - en-US-JennyNeural

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  create-full-video:
    runs-on: ubuntu-latest
    steps:
      - name: 📦 Checkout
        uses: actions/checkout@v5

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: ⬇️ Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg
          pip install requests "moviepy==1.0.3" "Pillow==9.5.0" edge-tts

      - name: 🎬 Generate video (Stocks + Voice, NO subtitles)
        env:
          PEXELS_KEY: ${{ secrets.PEXELS_API_KEY }}
          TOPIC: ${{ github.event.inputs.topic }}
          VOICE: ${{ github.event.inputs.voice }}
        run: |
          python - << 'PYTHON_SCRIPT'
          import requests, os, asyncio
          from moviepy.editor import VideoFileClip, AudioFileClip, concatenate_videoclips
          import edge_tts

          TOPIC = os.environ['TOPIC']
          VOICE = os.environ['VOICE']
          PEXELS_KEY = os.environ['PEXELS_KEY']

          # 1. Script for voiceover
          script = f"{TOPIC}. No internet. Zero RAM. Full privacy. Free 48 hours."

          # 2. Generate TTS
          async def gen_audio(text, voice, out):
              comm = edge_tts.Communicate(text, voice)
              await comm.save(out)
          asyncio.run(gen_audio(script, VOICE, 'voiceover.mp3'))

          # 3. Fetch stock videos from Pexels
          headers = {'Authorization': PEXELS_KEY}
          url = f"https://api.pexels.com/videos/search?query={TOPIC.replace(' ', '+')}&per_page=5&orientation=portrait"
          vids = requests.get(url, headers=headers).json().get('videos', [])
          
          os.makedirs('clips', exist_ok=True)
          clip_paths = []
          for i, v in enumerate(vids[:4]):
              p = f"clips/clip_{i}.mp4"
              with open(p, 'wb') as f:
                  f.write(requests.get(v['video_files'][0]['link']).content)
              clip_paths.append(p)

          # 4. Load audio
          audio = AudioFileClip('voiceover.mp3')
          dur = audio.duration

          # 5. Process clips: crop + resize to 9:16 (1080x1920)
          processed = []
          for cp in clip_paths:
              c = VideoFileClip(cp)
              target_w, target_h = 1080, 1920
              ratio = target_w / target_h
              if c.w / c.h > ratio:
                  new_w = int(c.h * ratio)
                  c = c.crop(x1=(c.w - new_w)//2, width=new_w, height=c.h)
              else:
                  new_h = int(c.w / ratio)
                  c = c.crop(y1=(c.h - new_h)//2, width=c.w, height=new_h)
              c = c.resize((target_w, target_h))
              processed.append(c)

          # 6. Loop clips to match audio duration
          from itertools import cycle
          final_clips = []
          t = 0
          for clip in cycle(processed):
              if t >= dur: break
              d = min(clip.duration, dur - t)
              final_clips.append(clip.subclip(0, d))
              t += d

          video = concatenate_videoclips(final_clips).set_audio(audio)

          # 7. Export (NO subtitles - avoid ImageMagick)
          os.makedirs('output', exist_ok=True)
          video.write_videofile('output/final_video.mp4', codec='libx264', audio_codec='aac', fps=30, logger=None)
          print('✅ Done: output/final_video.mp4')
          PYTHON_SCRIPT

      - name: 📤 Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: full-video
          path: output/final_video.mp4
          retention-days: 7
